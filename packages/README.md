# Packages

The MCP discovery package lives in its own repository:

**https://github.com/This-Is-Hellgate/secondeye-mcp**

Install in Cursor or Claude Desktop:

```json
{
  "command": "npx",
  "args": ["-y", "@secondeyes/mcp-unblock"]
}
```
